# Evals — noa-summary-udm

Evaluation cases for the NOA Summary UDM extraction contract go under `cases/`,
each with a `metadata.yaml` declaring `validated_against_version`. None are
committed yet; the workflow was validated end-to-end in Vandalizer against real
Notices of Award (see the AI4RA re-validation tracker). Add golden cases here
when wiring this component into the evaluation harness.
